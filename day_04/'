# you need to get all of the values you need in the same column together
# a = [
#     list(
#         zip(
#             *[list(map(int, line.split()))]
#             for line in open("test.txt").read().splitlines()
#         )
#     )
# ]

# a = list(
#     zip(
#         *[list(map(int, line.split())) for line in open("test.txt").read().splitlines()]
#     ),
# )
# print(*a)

t = list(map(int, line.strip()) for line in open("test.txt").read().splitlines())
print(t)
